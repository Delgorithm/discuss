const btn_cross = document.querySelector('.cross-exit');

btn_cross.addEventListener('click', function () {
    window.location.href = "/";
});

// By clicking on "Sign In" while Pseudonym and passwords are fill, it will redirect the user to its profil
const btn_signin = document.querySelector('.signin-btn')
